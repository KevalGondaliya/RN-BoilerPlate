import React, { FC } from 'react';
import { View, Text } from 'react-native';

import { styles } from './styles';

const Home: FC = () => {
  return (
    <View>
      <Text style={styles.txt}>Playlist screen</Text>
    </View>
  );
};

export default Home;
